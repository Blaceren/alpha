// SYNTHETIC MOCK DATA — NOT PRODUCTION.
// Deprecated shim: the dataset now lives in ./fixtures/ (directory).
// Kept so any older import path still resolves. Prefer "./fixtures/index".
export { defaultDataset, buildDataset } from "./fixtures/index";
