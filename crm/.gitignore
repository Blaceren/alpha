# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# typescript incremental build info (build artifact — do not commit)
*.tsbuildinfo

# production
/build

# testing
/coverage
/test-results
/playwright-report
/playwright/.cache
/tests-e2e/__screenshots__

# misc
.DS_Store
*.pem
*.log
npm-debug.log*

# env / secrets — never commit real secrets (Phase 1A uses none)
.env
.env.local
.env.*.local

# editor
.idea
.vscode

# stray sandbox artifacts (cannot be deleted on this mount)
*.zip
ziMfbTLc
