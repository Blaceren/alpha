// SAFE: this comment mentions $queryRaw, $executeRawUnsafe and Prisma.raw() as
// prose. None of them is a call site, so the auditor must report nothing here.
/* A block comment naming $queryRawUnsafe must also be ignored. */
export const note = "documentation only";
